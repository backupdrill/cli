node_modules/
dist/
*.log

# 本地配置与凭据:永不提交
backupdrill.config.json
.env
.env.*
!.env.example

# cross-review artifacts
.review/

# Supabase CLI 本地状态(含项目 ref):公开仓永不提交。
# 只忽略本地状态目录,不要写成 supabase/ —— 那会连迁移/函数一起吞掉。
/supabase/.temp/
